<view class="nav-conatiner custom-class" style="{{containerStyle}}">
  <van-icon wx:if="{{showBack}}" custom-class="back-icon" name="arrow-left" size="20px" bindtap="handleBack" />
  <text class="nav-title">{{title}}</text>
</view>